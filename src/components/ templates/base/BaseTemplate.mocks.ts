import { IBaseTemplate } from './BaseTemplate';

const BaseTemplateMock: IBaseTemplate = {
  sampleTextProp: 'Hello World',
};
const AltTemplateMock: IBaseTemplate = {
  sampleTextProp: 'Goodbye World',
};

export const MockBaseTemplate = { BaseTemplateMock, AltTemplateMock };
