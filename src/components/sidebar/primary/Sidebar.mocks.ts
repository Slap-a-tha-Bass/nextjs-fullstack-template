import { ISidebar } from './Sidebar';

const SidebarMock: ISidebar = {
  title: 'Components',
  arrayOfLinks: ['Cards', 'Nav', 'Sidebar'],
};
const AltSidebarMock: ISidebar = {
  title: 'Goodbye World',
};

export const MockSidebar = { SidebarMock, AltSidebarMock };
