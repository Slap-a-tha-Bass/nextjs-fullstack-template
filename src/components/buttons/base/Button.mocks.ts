import { IButton } from './Button';

const ButtonMock: IButton = {
  sampleTextProp: 'Hello World',
};
const AltTemplateMock: IButton = {
  sampleTextProp: 'Goodbye World',
};

export const MockButton = { ButtonMock, AltTemplateMock };
